# Dashboard Layout Demo

A prototype dashboard layout built with [GridStack.js](https://gridstackjs.com/). Widgets can be dragged, resized, and rearranged on a responsive grid.

## Running locally

Serve the project with any static file server, for example:

```bash
npx serve .
```

Then open the URL shown in your terminal.
